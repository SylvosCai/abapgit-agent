---
layout: default
title: <name> - <Short Description>
nav_order: <N>
parent: <Development|Explore|Setup|Utility> Commands
---

# <name> Command

## Overview

One paragraph: what it does, when to use it, key constraints (e.g. requires ABAP connection / runs offline).

## Command

```bash
# Basic usage
abapgit-agent <name>

# With options
abapgit-agent <name> --option value
```

## Prerequisites

- What must exist before running (config file, connection, etc.)

## Parameters

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `--option` | No | — | What it does |

---

## How It Works

Numbered steps describing the internal flow.

1. Step one
2. Step two

---

## Output

### Success

```
Expected output on success
```

### Error

```
Expected output on error
```

---

## Error Handling

| Error | Message |
|-------|---------|
| Condition | `Error message shown` |

---

## Example — Typical Workflow

```bash
abapgit-agent <name> --option value
```
